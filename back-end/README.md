### Notes:

- If you are server owner run - `python app.py` and not `python load_balancer.py` unless it's your IP mapped to `giganoto`.

- Read all mark down files in `tasks/` before running any code.
